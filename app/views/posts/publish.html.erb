<h1>Twitterに画像をぶん投げたよ</h1>
<div>
<%= link_to '戻る', posts_path %><br/><br/>
</div>
<h2>
<%
p @media_urls
buf = @media_urls.first[1]
%>
<a href="https://twitter.com/intent/tweet?text=<%= @post.title%> - <%= @post.author %> <%= view_url @post %> <%= buf %>"
target="_blank">Twitterに撒く</a>
</h2>
<div style="margin-top:20px">
<% @media_urls.each do |elem | %>
<div><%= image_tag(elem[0], options = {}) %></div>
<% end %>
</div>
